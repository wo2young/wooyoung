<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.human99.mappers.UserMapper">

	<!-- 로그인 시 아이디로 사용자 조회 -->
	<select id="findByLoginId" parameterType="string"
		resultType="kr.or.human99.dto.UserDTO">
		SELECT
		user_id,
		login_id,
		password,
		name,
		user_role,
		created_at,
		updated_at
		FROM user_t
		WHERE login_id = #{loginId}
	</select>

	<!-- 마이페이지용 조회 -->
	<select id="findById" parameterType="int"
		resultType="kr.or.human99.dto.UserDTO">
		SELECT user_id, login_id, name, user_role, password, created_at, updated_at
		FROM user_t
		WHERE user_id = #{userId}
	</select>

	<!-- 비밀번호 변경 -->
	<update id="updatePassword" parameterType="map">
		UPDATE user_t
		SET password = #{password},
		updated_at = SYSDATE
		WHERE user_id = #{userId}
	</update>

</mapper>
