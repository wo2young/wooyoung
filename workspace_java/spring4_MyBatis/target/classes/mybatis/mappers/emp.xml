<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.emp">

	<select id="selectEmp" resultType="kr.or.human4.dto.EmpDTO">
		select * from emp2
	</select>

	<!-- <select id="selectOneEmp" resultType="kr.or.human4.dto.EmpDTO"> -->
	<select id="selectOneEmp" resultType="empDTO">
		select * from emp2
		where
		empno = 1111
	</select>

	<!-- <select id="selectOneEmpMap" resultType="java.util.HashMap"> -->
	<select id="selectOneEmpMap" resultType="hashMap">
		select * from emp2
		where
		empno = 1111
	</select>

	<!-- <select id="selectEmpno" resultType="empDTO" parameterType="java.lang.Inreger"> -->
	<select id="selectEmpno" resultType="empDTO" parameterType="int">
		select * from emp2
		where empno = #{_parameter}
	</select>

	<select id="selectEname" resultType="empDTO"
		parameterType="String">
		select * from emp2
		where ename like '%' || #{_parameter}
		|| '%'
	</select>
	<!-- #{ } : ''를 자동으로 붙여 줌 where ename = 'KING' where empno = 1234 ${ } : 
		''를 안 붙임 where ename = KING where ename = '${_parameter}' -->

	<select id="getSeqEmp2" resultType="int">
		select seq_emp2.nextval from dual;
	</select>

	<!-- empno는 seq에서 따오고 나머지 정보를 넣는 insert 완성 -->
	<insert id="insertEmp2" parameterType="empDTO">
		INSERT INTO emp2 (empno, ename, job, mgr, hiredate, sal, comm, deptno)
		VALUES (seq_emp2.nextval, #{ename}, #{job}, #{mgr}, #{hiredate},
		#{sal}, #{comm}, #{deptno})
	</insert>
</mapper>




